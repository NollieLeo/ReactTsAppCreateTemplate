{
  "plugins": [["@babel/plugin-transform-runtime"]],
  "presets": [
    ["@babel/preset-env"],
    [
      "@babel/preset-react"
      // {
      //   "runtime": "automatic"
      //   // 这一行可以在rect hook 版本里不引入import React from 'react'
      // }
    ]
  ]
}
